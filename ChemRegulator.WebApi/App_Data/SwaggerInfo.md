To ensure that only authenticated users can access the Web API, all actions are protected using [JWT based authorization](https://jwt.io/introduction/).

To retrieve an access token, use the `api/tokens` endpoint.

